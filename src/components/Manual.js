import React from "react";

export default function Manual(props) {
  return (
    <div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
      <div className={`my-3  px-3 py-3 ${props.mode}`}>
        <h2>Preview :-</h2>
        <p>{}</p>
      </div>
    </div>
  );
}
